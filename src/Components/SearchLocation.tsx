import React from "react"
import { Location } from "@Types/Location"

const SearchLocation: React.FC<{onSelectLocation: (location: Location)}> = ({onSelectLocation}) => {
  return null
}

export default React.memo(SearchLocation)